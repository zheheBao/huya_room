import { Dimensions, Platform } from "react-native";
export default function scalePx(px, base, force) {
  if (base === void 0) {
    base = 750;
  }

  var deviceH = Dimensions.get('window').height;
  var deviceW = Dimensions.get('window').width;
  var portialDeviceWidth = force || Platform.OS === 'web' ? deviceW : Math.min(deviceH, deviceW);
  return Math.ceil(px * portialDeviceWidth / base);
}